package dinic.java.simple;

/**
 * Used to perform the Dinic Algorithm using adjacency matrices.
 * For a faster implementation, see @see ../fast/Dinic.java (using adjacency Lists)
 */
public class Dinic {
    /**
     * Implementation of Dinic using adjacency matrix.
     * It uses O(|V|^3) time, where |V| is the number of nodes.
     * @param graph an adjacency-matrix-representation of the flow graph where (x,y) is the capacity of the edge or 0 if there is no edge.
     * @param from the starting node from which to calculate the flow
     * @param to the ending node to which to calculate the flow
     * @return the flow from node "to" to node "from"
     */
    public static int dinic(int[][] graph, int from, int to) {
        //TODO
        return 0;
    }
}
